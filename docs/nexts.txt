###Backlog
[chain:5bcbb9918ea0] Permission request flow fix: when auto-approval grants permission, avoid leaving the project stale. Either remove/suppress the "block request permission" message, or have orchestrator post in the same channel that permission was conceded/granted, then automatically resume the waiting work instead of staying blocked.
